#!/bin/bash
# Sync installed package list to repo
set -euo pipefail

REPO_DIR="$(cd "$(dirname "$0")/.." && pwd)"
PKG_LIST="$REPO_DIR/packages_list"

new=$(pacman -Qqe | sort) || exit 1

if ! printf '%s\n' "$new" | cmp -s - "$PKG_LIST"; then
    printf '%s\n' "$new" | tee "$PKG_LIST" >/dev/null
fi
